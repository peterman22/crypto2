interface IProps { }


const SupportedCoins = (props: IProps) => {
    return (
        <section className={`border-t-2 border-b-2 border-yellow-500`}>

        </section>
    );
}

export default SupportedCoins;